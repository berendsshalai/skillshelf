# SkillShelf 0.3.0 resolution matrix

Starting commit: `b43e6b5c5a699b6b3d323ff850dc02dddc56f52d`

This matrix is the release evidence index. A row remains `PENDING` until the
named test or reproducible command passes. Optional providers are never treated
as connected from configuration alone.

| Finding | Previous state | Implemented correction | Files changed | Test proving correction | Remaining limitation |
| --- | --- | --- | --- | --- | --- |
| Runtime and maintenance agents shared an ambiguous plane | Fourteen definitions were in default discovery | Moved eight maintenance sources and added runtime, maintenance and all profiles | `agents/maintenance/`, `scripts/generate-agents.py` | Profile counts `6/8/14`; runtime `--check` passes | Maintenance generation is opt-in |
| Delegations used arbitrary strings | `Agent.as_tool()` accepted the SDK default string | Added typed `DelegationInput`, bounded prompt builder and schema-bearing agent tools | `runtime/delegation.py`, `orchestrator.py` | `test_runtime_contracts.py` | Credentialled master use was not run |
| Specialists had no real function tools | Orchestrator passed empty tool lists | Capability registry now builds SDK function tools for filesystem and safe commands | `tools/`, `orchestrator.py` | `test_tools_operational.py`; deep doctor function call PASS | Browser and connector capabilities remain host-provided |
| Specialist output trusted model evidence | Model supplied execution claims | Added runtime evidence contracts and mechanical overwrite helper | `runtime/results.py` | `test_runtime_contracts.py` | Full model-run merge remains credentialled-only |
| MCP registry was metadata-only | Definitions were never connected | Added typed SDK factory/manager, filtering, cache, timeout and clean shutdown | `mcp/`, `mcp_runtime.py`, `mcp/registry.yml` | `test_mcp_operational.py` | Optional live servers are SKIPPED until available |
| MCP approvals were booleans | `approved=True` bypassed policy | Removed boolean bypass and added durable argument-bound approval records | `runtime/approvals.py`, `mcp/approvals.py` | Runtime/MCP approval tests | SDK resume is tested locally; live write providers are deferred |
| Registry budgets were not authoritative | One global pre-call hook existed | Specialist output caps and request/agent/tool budget ledger are wired | `agent_factory.py`, `runtime/budgets.py`, `orchestrator.py` | `test_runtime_contracts.py` | Paid A/B measurement requires `OPENAI_API_KEY` |
| Session history was split | Metadata and SDK history used separate DBs | Unified session manager uses one SQLite file and verifies redacted deletion | `sessions/`, `cli.py` | `test_sessions_operational.py`; deep doctor round-trip PASS | Path fallback remains without Git metadata |
| Repository identity used the local path | Reclones produced unrelated prefixes | Normalized origin URL is authoritative when available | `sessions/identity.py` | `test_sessions_operational.py` | Local-only repositories retain path fallback |
| No durable run ledger | Success-only JSON files existed | Added SQLite lifecycle, usage, tools and error records before model execution | `ledger/`, `orchestrator.py`, `cli.py` | `test_ledger_operational.py`; doctor ledger PASS | PostgreSQL is deferred |
| Guardrails were regex-only | Narrow input/output patterns | Tool confinement, argv policy, MCP filtering and connector network policy add enforcement layers | `tools/`, `mcp/`, `connectors/rest.py` | Security/tool/connector tests | Output scanner remains pattern-based |
| Governance apply was not an atomic full-package transaction | One file was copied live before tests | Complete-package staging, pre-apply tests, lock, backup, atomic swap, journal, recovery and rollback | `governance/`, `improvement.py` | `test_governance_safety.py` (14 cases) | Live update still requires explicit approval |
| Version was duplicated | Four surfaces drifted | Added `VERSION` and consistency validator across manifests, package, site and changelog | `VERSION`, manifests, `validate-version.py` | Version consistency PASS | Generated surfaces remain CI-enforced |
| Installer used the source tree | Editable install under checkout | Verifies lock digest, builds and installs a wheel into an isolated versioned environment | installer/uninstaller scripts | Real Windows install, deep doctor, uninstall twice PASS | Linux CI execution remains required |
| Doctor checked declarations, not operations | YAML presence appeared healthy | Deep mode executes function tool, session lifecycle, ledger, governance and databases | `cli.py`, `mcp_runtime.py` | `skillshelf doctor --deep --json` | Model and optional MCP calls are SKIPPED without explicit credentials |
| No connector SDK | External integration absent | Added typed manifests, env secrets and safe REST paging/retry/ETag client | `connectors/` | `test_integration_connector.py` | OpenAPI importer and live providers are NOT COMPLETED |
| No canonical data plane | No raw/canonical/quarantine records | Added SQLite immutable raw, normalized entities, lineage and quarantine | `data/` | `test_integration_data_pricing.py` | PostgreSQL is NOT COMPLETED |
| No deterministic pricing engine | No authoritative arithmetic | Decimal formula, rounding and evidence persistence added | `pricing/` | Golden ZAR `156.25` test | Policy configuration remains user-owned |
| No delivery abstraction | No quote model | Deterministic business-day provider separates handling and transit range | `delivery/` | Delivery integration test | Live carriers are deferred |
| No communications gateway | No consent or delivery state | Consent/authority gates, mock email/WhatsApp and monotone webhook reducer added | `communications/` | Communications integration test | SMTP, Meta and Twilio live adapters are NOT COMPLETED |
| No durable workflow engine | Conversation was the only state | SQLite stock-to-offer checkpoints, idempotency, restart and reconciliation added | `workflows/` | `test_integration_stock_to_offer.py` | Generic scheduler/retry/DLQ CLI is partial |
| No stock-to-offer proof | Chain did not execute | Loopback stock through data, price, delivery, two mock channels and webhooks | Integration packages | Exact E2E test passes with run `run-1` | Local mocks do not prove live accounts |
| No deterministic capability suggestions | Gaps were not detected | Added ordered fact-based suggestions | `suggestions/` | Integration suggestion assertions | Suggestions never install or send |
| No bounded RAG service | No retrieval backend | Added SQLite FTS5 ingestion, freshness and citations | `rag/` | RAG integration test | Model synthesis is optional |
| No local control API | No endpoints | Added optional FastAPI health, connectors, runs, webhooks, usage and suggestions | `api/`, Docker files | `test_integration_api.py` | Live sync/run endpoints and public auth are NOT COMPLETED |
| Credentialled evaluation was one prompt | No mechanical scoring | Added eight fixtures, ceilings, scoring and redacted reports | credentialled eval script, fixture and workflow | Offline fixture validation PASS | Paid suite NOT RUN; manual only |
| Release artifacts were incomplete | Plugin only | Builds plugin, wheel, sdist, runtime/source, checksums, SPDX and provenance | release scripts/workflow | Real artifact build and strict validator PASS | Signing credentials are deferred |
| CI matrix was incomplete | One Python per OS | Added Windows/Ubuntu Python 3.11/3.13 runtime matrix and reports | `agent-runtime.yml` | Local equivalent gates pass | GitHub CI is NOT RUN on this unpushed branch |
| Agent documentation was skeletal | Three-line guides | Replaced 21 operational agent/integration guides | `docs/agents/`, `docs/integrations/` | Required heading verification PASS | Provider-specific setup awaits live adapters |

## Baseline evidence

Raw starting-state logs are preserved in `work/audit/baseline/`. The system
Python run records missing runtime dependencies, while the isolated project
environment records 34 repository tests, 22 SDK tests and 9 memory tests
passing before the 0.3.0 implementation.
