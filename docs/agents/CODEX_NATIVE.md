# Native Codex agents

Run `python scripts/generate-agents.py`. Six project-scoped TOML definitions are emitted to `.codex/agents/` and mirrored under `agents/generated/codex/`. Current Codex requires `name`, `description` and `developer_instructions`; sandbox configuration is added per role.
