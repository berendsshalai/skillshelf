# Python SDK

The package is `skillshelf-agents`; imports use `skillshelf_agents`; the CLI is `skillshelf`. Model-backed `ask` and `run` use the OpenAI Agents SDK and require `OPENAI_API_KEY`. Inspection, generation, evaluation and policy commands are offline.
