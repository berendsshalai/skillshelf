# Token economy

The SDK records requests, input, output, cached and reasoning tokens without prompt content. Soft limits stop optional calls; hard limits stop delegation and return truthful partial results. `agents/generated/evaluation-report.json` compares eager all-skill bytes with lazy selected-skill bytes. This is a deterministic context estimate, not a paid-model usage claim.
