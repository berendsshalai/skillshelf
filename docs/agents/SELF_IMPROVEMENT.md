# Self-improvement

Run observations are deterministic and cost no extra model call. Auto-review defaults off. Proposals are staged beneath `SKILLSHELF_HOME/staged-updates`; approval backs up the live file, applies the stage, runs tests and rolls back on failure. There is no autonomous merge or publication.
