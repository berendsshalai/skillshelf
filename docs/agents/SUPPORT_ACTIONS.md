# Support actions

`skillshelf star --yes` is the only GitHub engagement action. It requires explicit consent and authenticated `gh`, is idempotent and verifies the mutation. Installation, cloning, `ask`, agents and governance never star or follow repositories.
