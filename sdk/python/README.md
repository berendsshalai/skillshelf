# SkillShelf Agents

Install with `python -m pip install .` and run `skillshelf doctor`. Model-backed `ask` and `run` commands require `OPENAI_API_KEY`; inspection, routing, evaluation, usage, sessions, MCP policy, support, and generation commands work without a model call.
