# Upstream difference

No vendored module is rewritten. SkillShelf adds one discovery router, Codex tool mappings, Windows guidance, an explicit authority boundary, and bounded delegation. Claude session-start injection is intentionally not installed.
